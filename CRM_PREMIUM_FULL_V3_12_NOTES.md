# CRM Premium Full V3.12 Notes

## Forecast export header fix
- Forecast report CSV export column title changed from `Tarih Text` to `Forecast Dönemi`.
- Excel text protection remains active, so values such as `Aralık 2026` are preserved and not converted to `Ara26`.

## Build check
- `npm run check:build` passed.
